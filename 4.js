// Write the differences of let and var keywords ( 2 points ).

console.log("difference between var And let")

console.log("var.1 -> The variables that are defined with var statement have function scope.")
 console.log("let.1 -> The variables that are defined with let statement have block scope.")

 console.log("var.2 -> We can declare a variable again even if it has been defined previously in the same scope.")
 console.log("let.2 -> We cannot declare a variable more than once if we defined that previously in the same scope.")
